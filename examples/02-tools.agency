def getCapital(country: string): string {
  if (country == "India") {
    return "New Delhi"
  } else {
    return "Unknown"
  }
}

node main() {
  const result = llm(
    "Use your `getCapital` tool to find the capital of India",
    {
    tools: [getCapital]
  },
  )
  print(result)
}
